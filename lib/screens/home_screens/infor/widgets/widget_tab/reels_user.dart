import 'package:flutter/material.dart';

class ReelsUser extends StatelessWidget {
  const ReelsUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nội dung Tab 2'),
    );
  }
}
